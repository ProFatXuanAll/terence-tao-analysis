# latexindent settings for this project.
# latexindent ships with `defaultIndent: "\t"' (a tab); this repo uses two
# spaces, so override it here.  Applied via `make fmt' (latexindent -l ...).
defaultIndent: "  "
