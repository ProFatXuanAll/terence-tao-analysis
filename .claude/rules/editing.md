# Editing notes

Author is non-native English and self-taught in math; grammar, fluency, and rigor fixes are welcome but preserve voice and statement coverage.
Before "correcting" a statement, check Tao's errata (linked in [README.md](../../README.md)) — the difference may be intentional.

`.tex` files are formatted with `latexindent` (default configuration); run it before committing.

Markdown source style: one sentence per line.
For multi-sentence list items, use a 2-space-indented continuation line per sentence so each sentence sits on its own source line while the bullet renders as one item.
